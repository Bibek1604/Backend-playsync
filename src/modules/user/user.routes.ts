/**
 * User Module - Routes
 * Main router for user-related endpoints
 * Note: history and scorecard are now independent modules
 */

import { Router } from 'express';

const router = Router();

// User-specific routes can be added here in the future
// Example: profile management, settings, etc.

export default router;
